{
	"name": "easy-invoice",
	"version": "1.0.0",
	"description": "",
	"main": "backend/dist/main",
	"scripts": {
		"start:prod": "cd backend && npm run start:prod",
    "build": "cd backend && npm run build",
    "postinstall": "cd backend && npm install && npm run build"
	}
}
